import { render } from '@testing-library/vue'
import Component from "@/components/BaseInputToggle.vue";

describe("components/BaseInputToggle.vue", () => {
  test.todo("implement tests!")
  test("", () => {
    const { getByText } = render(Component)
  });
});
